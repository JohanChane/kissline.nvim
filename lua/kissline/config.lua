local default_opts = {
  statusline = {
    enable = true,
  },
  tabline = {
    enable = true,
  },
  bufline = {
    enable = false,
  },
}

local setup_opts = {}

return {
  default_opts = default_opts,
  setup_opts = setup_opts
}
